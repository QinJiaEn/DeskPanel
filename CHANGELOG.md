# Changelog

## v2.0 (2026-05-13)

### 新增功能

- **AI 智能分类** — 收纳桌面时可调用 AI（OpenAI 兼容接口）分析文件名，自动推荐分类。连点 5 次标题栏 "DeskPanel · Mr.Q" 输入访问码开启
- **背景图片自定义** — 设置中可选择图片作为面板背景，子区域自动半透明显示
- **归还桌面** — 新增 "归还桌面" 按钮，将已收纳的文件一键归还到桌面
- **窗口恢复按钮** — 窗口最大化时标题栏显示恢复按钮，点击回到默认大小和位置
- **边缘吸附循环隐藏/显示** — 窗口靠边隐藏后，鼠标移到边缘可反复弹出和隐藏，支持快捷键和托盘触发后鼠标离开自动隐藏

### 改进

- 文件卡片图标尺寸增大（32x32 → 42x42），文件夹图标同步放大
- Debug 模式输出控制台日志，方便排查 AI 分类问题
- Release 模式打包为 WinExe（无控制台窗口）
- 支持 `dotnet publish` 自包含单文件 exe 发布

### 修复

- 修复退出时 `Mutex.ReleaseMutex` 导致的崩溃
- 修复 `DragMove` 未判断鼠标按键状态导致的异常
- 修复启动时静默崩溃，添加全局异常处理
- 修复确认按钮无防重入锁导致的二次点击崩溃
- 修复单文件发布后图标加载失败（改用 `AppContext.BaseDirectory`）
- 修复标题栏 "DeskPanel · Mr.Q" 点击事件被 `DragMove` 吞掉的问题

---

## v1.0

初始版本：桌面文件收纳面板，支持分类管理、拖拽收纳、搜索、边缘吸附、系统托盘等基础功能。
